To configure this module:

1. Go to *Settings > Contracts*.
2. Use *Create recurring invoices* to enable or disable recurring invoice
   generation for the current company (enabled by default).
3. When enabled, set an optional domain to restrict which contracts of the
   company are invoiced by the cron.

The setting is per company, so switch the active company to configure another
one.
