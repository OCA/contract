This module lets each company control which of its contracts are picked up by
the recurring invoices cron.

Per company you can:

- Disable recurring invoice generation entirely.
- Restrict generation to the contracts matching an extra domain, on top of the
  domains contributed by the standard flow and other contract modules.
