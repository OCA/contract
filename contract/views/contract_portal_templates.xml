<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2020 Tecnativa - Víctor Martínez
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>
    <template
        id="portal_my_home_menu_contract"
        name="Portal layout : Contract menu entries"
        inherit_id="portal.portal_breadcrumbs"
        priority="35"
    >
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li
                t-if="page_name == 'Contracts'"
                t-attf-class="breadcrumb-item #{'active ' if not contract else ''}"
            >
                <a
                    t-if="contract"
                    t-attf-href="/my/contracts?{{ keep_query() }}"
                >Contracts</a>
                <t t-else="">Contracts</t>
            </li>
            <li t-if="contract" class="breadcrumb-item active">
                <t t-esc="contract.name" />
            </li>
        </xpath>
    </template>
    <template
        id="portal_my_home_contract"
        name="Show Contracts"
        inherit_id="portal.portal_my_home"
        customize_show="True"
        priority="30"
    >
        <xpath expr="//div[hasclass('o_portal_docs')]" position="before">
            <t t-set="portal_client_category_enable" t-value="True" />
        </xpath>
        <div id="portal_client_category" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t
                    t-set="icon"
                    t-value="'/contract/static/src/img/contract_icon.svg'"
                />
                <t t-set="title">Contracts</t>
                <t t-set="url" t-value="'/my/contracts'" />
                <t t-set="text">Follow your contracts</t>
                <t t-set="placeholder_count" t-value="'contract_count'" />
            </t>
        </div>
    </template>
    <template id="portal_my_contracts" name="My Contracts">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True" />
            <t t-call="portal.portal_searchbar">
                <t t-set="title">Contracts</t>
            </t>
            <t t-if="contracts" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Contract #</th>
                        <th class='d-none d-md-table-cell'>Date</th>
                        <th class='d-none d-md-table-cell'>Date end</th>
                        <th class='text-right'>Reference</th>
                    </tr>
                </thead>
                <tbody>
                    <t t-foreach="contracts" t-as="contract">
                        <tr>
                            <td>
                                <a
                                    t-att-href="contract.get_portal_url()"
                                    t-attf-class="tr_contract_link"
                                    t-att-title="contract.name"
                                >
                                    <t t-esc="contract.name" />
                                </a>
                            </td>
                            <td class="d-none d-md-table-cell">
                                <span t-field="contract.recurring_next_date" />
                            </td>
                            <td class="d-none d-md-table-cell">
                                <span t-field="contract.date_end" />
                            </td>
                            <td class='text-right'>
                                <span t-field="contract.code" />
                            </td>
                        </tr>
                    </t>
                </tbody>
            </t>
        </t>
    </template>
    <template
        id="portal_contract_page"
        name="Contract"
        inherit_id="portal.portal_sidebar"
        primary="True"
    >
        <xpath expr="//div[hasclass('o_portal_sidebar')]" position="inside">
            <t
                t-set="o_portal_fullwidth_alert"
                groups="account.group_account_invoice,account.group_account_readonly"
            >
                <t t-call="portal.portal_back_in_edit_mode">
                    <t
                        t-set="backend_url"
                        t-value="'/odoo/action-contract.action_customer_contract/%s' % contract.id"
                    />
                </t>
            </t>

            <div class="row o_portal_contract_sidebar">
                <!-- Sidebar -->
                <t t-call="portal.portal_record_sidebar">
                    <t t-set="classes" t-value="'col-lg-4 col-xxl-3 d-print-none'" />
                    <t t-set="title">
                        <h2 class="mb-0 text-break mx-auto">
                            <span t-field="contract.name" />
                        </h2>
                    </t>
                </t>

                <!-- Page Content -->
                <div
                    id="contract_content"
                    class="o_portal_content col-12 col-lg-8 col-xxl-9"
                >
                    <t t-if="error or warning" t-call="account.portal_contract_error" />
                    <t
                        t-if="success and (not error and not warning)"
                        t-call="account.portal_contract_success"
                    />

                    <div
                        class="o_portal_html_view position-relative bg-white shadow overflow-hidden"
                    >
                        <div class="o_portal_html_loader text-center">
                            <i
                                class="fa fa-circle-o-notch fa-spin fa-2x fa-fw text-black-50"
                            />
                        </div>
                        <iframe
                            id="contract_html"
                            class="position-relative d-block"
                            width="100%"
                            height="100%"
                            frameborder="0"
                            scrolling="no"
                            t-att-src="contract.get_portal_url(report_type='html')"
                        />
                    </div>
                    <!-- chatter -->
                    <div id="contract_communication" class="mt-4">
                        <h3>Communication history</h3>
                        <t t-call="portal.message_thread" />
                    </div>
                </div>
            </div>
        </xpath>
    </template>

    <template id="portal_contract_error" name="Contract error/warning display">
        <div class="row mr16">
            <div
                t-attf-class="'col-lg-12 mr16 ml16 alert alert-dismissable' #{'alert-danger' if error else 'alert-warning'}"
                role="alert"
            >
                <a
                    href="#"
                    class="close"
                    data-bs-dismiss="alert"
                    aria-label="close"
                    title="close"
                >×</a>
                <t t-if="error == 'generic'" name="generic">
                    There was an error processing this page.
                </t>
            </div>
        </div>
    </template>

    <template id="portal_contract_success" name="Contract success display">
        <div class="row mr16">
            <div
                class="col-lg-12 mr16 ml16 alert alert-dismissable alert-success"
                role="status"
            >
                <a
                    href="#"
                    class="close"
                    data-bs-dismiss="alert"
                    aria-label="close"
                    title="close"
                >×</a>
            </div>
        </div>
    </template>
</odoo>
